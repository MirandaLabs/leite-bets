# --- CONEXÃO COM O BANCO DE DADOS (RAILWAY) ---
# Substitui pelos dados que aparecem no teu painel do Railway em:
# PostgreSQL -> Connect -> Postgres Connection URL (Pública)
# IMPORTANTE: Garante que começa com postgresql+psycopg://
DATABASE_URL=postgresql+psycopg://postgres:SUA_SENHA_AQUI@roundhouse.proxy.rlwy.net:PORTA_AQUI/railway

# --- URL DA API NO RAILWAY (OBRIGATÓRIO PARA SCRAPERS LOCAIS) ---
# Esta URL recebe os dados coletados pelos scrapers locais
API_URL=https://leite-bets-production.up.railway.app/api/odds/scraper

# --- CONFIGURAÇÕES DO SCRAPER ---
# Porta onde a API local vai responder (usada pelo n8n local)
PORT=8000
LOG_LEVEL=INFO

# --- PROXY WEBSHARE (OPCIONAL NO LOCAL) ---
# Se quiseres usar a tua internet real (Brisanet), deixa estas linhas comentadas com #
# Se quiseres usar o proxy residencial, preenche com os teus dados base:
WEBSHARE_USERNAME=zecdovnb
WEBSHARE_PASSWORD=4zcf0mk9pkmy
WEBSHARE_HOST=p.webshare.io
WEBSHARE_PORT=80

# --- URL DO BACKEND NA NUVEM ---
# O n8n local usará isto para enviar alertas ou triggers para a nuvem
REMOTE_API_URL=https://leite-bets-production.up.railway.app
